"""
TRACE: attribute-inference attack and evaluation machinery.

Placeholder — nothing has moved in here yet. The implementation currently lives in the
reproduction scripts (`repro/cue_tagger.py`, `repro/synthpai_eval.py`, `repro/scoring.py`);
promote it here when it settles into library code. The corpora it evaluates against are
already packaged, in `fusit.dataset`.
"""
